---
phase: 02-signal-infrastructure
plan: 04
type: execute
wave: 1
depends_on: ["02-03"]
files_modified:
  - lib/loomkin_web/live/workspace_live.ex
autonomous: true
gap_closure: true
requirements:
  - FOUN-02
  - FOUN-03

must_haves:
  truths:
    - "workspace_live has zero direct Jido Signal Bus subscriptions — all signal delivery goes through TeamBroadcaster"
    - "All topic strings in workspace_live use Topics module functions — no raw string literals in subscribe calls"
  artifacts:
    - path: "lib/loomkin_web/live/workspace_live.ex"
      provides: "Clean workspace with no direct bus subscriptions"
      contains: "No Loomkin.Signals.subscribe calls"
  key_links:
    - from: "lib/loomkin_web/live/workspace_live.ex"
      to: "lib/loomkin/teams/team_broadcaster.ex"
      via: "All signal delivery exclusively through TeamBroadcaster"
      pattern: "TeamBroadcaster\\.subscribe"
---

<objective>
Remove the two remaining direct Jido Signal Bus subscriptions from workspace_live.ex to complete the clean break required by Phase 2 success criteria.

Purpose: The verification report found two `Loomkin.Signals.subscribe` calls that bypass TeamBroadcaster — one for `session.**` (duplicate, already covered by TeamBroadcaster's global_bus_paths) and one for `collaboration.vote.*` (already covered by `collaboration.**` in global_bus_paths). Both cause duplicate signal delivery and violate the "zero direct bus subscriptions" criterion.

Output: workspace_live.ex with no direct Signals.subscribe calls
</objective>

<execution_context>
@/Users/vinnymac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vinnymac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-signal-infrastructure/02-03-SUMMARY.md
@.planning/phases/02-signal-infrastructure/02-VERIFICATION.md

<interfaces>
<!-- Key code locations in workspace_live.ex that need modification -->

Line 178 (inside start_and_subscribe/3, within `if connected?(socket)` block):
```elixir
# Subscribe to session signals via the Bus
Loomkin.Signals.subscribe("session.**")
```
This is immediately followed by TeamBroadcaster startup (lines 180-188) which already subscribes to session.** via Topics.global_bus_paths(). Safe to delete — session signals already flow through broadcaster.

Lines 3876-3885 (inside a handler for collaboration vote setup):
```elixir
# Subscribe to vote signals (only once to prevent duplicate delivery)
vote_topic = "ask_user:vote:#{question_id}"

socket =
  if socket.assigns[:vote_signals_subscribed] do
    socket
  else
    Loomkin.Signals.subscribe("collaboration.vote.*")
    assign(socket, vote_signals_subscribed: true)
  end
```
TeamBroadcaster already subscribes to `collaboration.**` which covers `collaboration.vote.*`. The on-demand subscription is redundant. Remove the subscribe call and the `vote_signals_subscribed` guard. The `vote_topic` variable for Phoenix PubSub (if used below) should remain — only the Jido bus subscribe is removed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate session.** and collaboration.vote.* direct bus subscriptions</name>
  <files>lib/loomkin_web/live/workspace_live.ex</files>
  <action>
Two targeted deletions in workspace_live.ex:

1. **Line 178 area** — Remove the two lines:
   ```elixir
   # Subscribe to session signals via the Bus
   Loomkin.Signals.subscribe("session.**")
   ```
   These sit inside `start_and_subscribe/3` in the `if connected?(socket)` block, right before the TeamBroadcaster startup. Session.subscribe(session_id) on line 175 is a Phoenix PubSub call and MUST stay — only the Jido bus subscribe on line 178 is removed.

2. **Line 3883 area** — Remove the on-demand Jido bus subscription block. The entire conditional block:
   ```elixir
   socket =
     if socket.assigns[:vote_signals_subscribed] do
       socket
     else
       Loomkin.Signals.subscribe("collaboration.vote.*")
       assign(socket, vote_signals_subscribed: true)
     end
   ```
   should be removed because `collaboration.**` in TeamBroadcaster's global_bus_paths already covers `collaboration.vote.*`. The `vote_topic` Phoenix PubSub variable above it should remain if it is used for PubSub subscription below. Check whether `vote_signals_subscribed` is referenced anywhere else and clean up any remaining references.

3. Also remove the `vote_signals_subscribed` assign from any initial assigns if present (check mount or start_and_subscribe for `vote_signals_subscribed: false`).

After removal, run `mix format` to ensure clean formatting.
  </action>
  <verify>
    <automated>cd /Users/vinnymac/Sites/vinnymac/loomkin && grep -n "Loomkin.Signals.subscribe" lib/loomkin_web/live/workspace_live.ex | grep -v "^#" ; echo "EXIT:$?"</automated>
  </verify>
  <done>grep returns zero matches for Loomkin.Signals.subscribe in workspace_live.ex — no direct bus subscriptions remain. mix format passes. mix compile --warnings-as-errors passes.</done>
</task>

<task type="auto">
  <name>Task 2: Validate full test suite passes with subscriptions removed</name>
  <files>lib/loomkin_web/live/workspace_live.ex</files>
  <action>
Run the full test suite to confirm removing the duplicate subscriptions causes no regressions. The key tests to watch:

1. `mix test test/loomkin/teams/team_broadcaster_test.exs` — broadcaster unit tests
2. `mix test test/loomkin/teams/team_broadcaster_integration_test.exs` — broadcaster integration tests
3. `mix test test/loomkin/teams/comms_test.exs` — comms subscription tests (includes source-scan for raw strings)
4. `mix test test/loomkin_web/live/workspace_live_test.exs` — workspace_live tests if they exist
5. `mix test` — full suite

If any test references `vote_signals_subscribed` in assertions, update it to reflect removal.

Also verify no compilation warnings about unused aliases or variables introduced by the removal. Run `mix compile --warnings-as-errors`.
  </action>
  <verify>
    <automated>cd /Users/vinnymac/Sites/vinnymac/loomkin && mix compile --warnings-as-errors && mix test test/loomkin/teams/ --max-failures 3 && mix test test/loomkin_web/live/ --max-failures 3</automated>
  </verify>
  <done>All tests pass. No compilation warnings. Zero direct Signals.subscribe calls in workspace_live.ex. The comms_test source-scan test (which checks for raw string absence) passes.</done>
</task>

</tasks>

<verification>
1. `grep -rn "Loomkin.Signals.subscribe" lib/loomkin_web/live/workspace_live.ex` returns no matches
2. `grep -rn "vote_signals_subscribed" lib/loomkin_web/live/workspace_live.ex` returns no matches
3. `mix compile --warnings-as-errors` passes
4. `mix test` passes (full suite)
</verification>

<success_criteria>
- workspace_live.ex contains zero calls to Loomkin.Signals.subscribe — all signal delivery goes through TeamBroadcaster exclusively
- No raw topic string literals remain in workspace_live.ex subscribe calls
- All existing tests pass without regression
- VERIFICATION.md truth #11 ("workspace_live has no direct Jido Signal Bus subscriptions") now passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-signal-infrastructure/02-04-SUMMARY.md`
</output>
