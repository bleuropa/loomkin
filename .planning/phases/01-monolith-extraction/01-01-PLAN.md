---
phase: 01-monolith-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/loomkin_web/live/command_palette_component.ex
  - test/loomkin_web/live/command_palette_component_test.exs
autonomous: true
requirements:
  - FOUN-01

must_haves:
  truths:
    - "Command palette opens on Cmd+K keyboard shortcut (handled via keyboard_shortcut event)"
    - "User can search agents, tabs, and actions via the palette search input"
    - "Selecting a palette item closes the palette and executes the action"
    - "Pressing Escape closes the palette"
    - "Palette renders zero results state when no match found"
  artifacts:
    - path: "lib/loomkin_web/live/command_palette_component.ex"
      provides: "Self-contained LiveComponent with palette state and events"
      exports: ["LoomkinWeb.CommandPaletteComponent"]
    - path: "test/loomkin_web/live/command_palette_component_test.exs"
      provides: "Render-level verification of palette states"
  key_links:
    - from: "lib/loomkin_web/live/command_palette_component.ex"
      to: "lib/loomkin_web/live/workspace_live.ex"
      via: "send/notify parent for navigation actions (palette_select triggers push_navigate or assign changes in parent)"
      pattern: "send_update.*WorkspaceLive|Phoenix.PubSub|send.*self"
---

<objective>
Extract the inline command palette render and event logic from workspace_live.ex into a standalone
CommandPaletteComponent LiveComponent.

Purpose: Remove ~150 lines of palette logic from the monolith and give it self-contained state
(open flag, query, results). This is a pure behavior-preserving extraction.

Output: lib/loomkin_web/live/command_palette_component.ex with full palette logic, plus a render test.
</objective>

<execution_context>
@/Users/vinnymac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vinnymac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monolith-extraction/01-CONTEXT.md

@lib/loomkin_web/live/workspace_live.ex

<interfaces>
<!-- Key patterns the executor needs. All extracted from workspace_live.ex. -->

Palette state in workspace_live.ex mount/2:
```elixir
command_palette_open: false,
command_palette_query: "",
command_palette_results: [],
```

Palette events currently in workspace_live.ex (lines 641-778):
- handle_event("keyboard_shortcut", %{"key" => "command_palette"}, socket)
  → toggles command_palette_open, runs palette search if opening
- handle_event("palette_search", %{"value" => query}, socket)
  → searches agents, tabs, actions, updates command_palette_results
- handle_event("palette_select", %{"type" => type, "value" => value}, socket)
  → multiple clauses for "agent", "tab", "action", "sub_tab" types
  → closes palette and triggers navigation/assign changes
- handle_event("close_command_palette", _params, socket)
  → sets command_palette_open: false

Palette render at line 4602:
```elixir
defp render_command_palette(assigns) do
  # uses @command_palette_open, @command_palette_query, @command_palette_results
  # phx-hook="CommandPalette" on the inner div (id="command-palette")
  # palette_select events use phx-value-type and phx-value-value
end
```

Called from render/1 at line 2254:
```elixir
{render_command_palette(assigns)}
```

Component pattern in this codebase:
```elixir
defmodule LoomkinWeb.SomeComponent do
  use LoomkinWeb, :live_component

  def update(assigns, socket) do
    {:ok, assign(socket, assigns)}
  end

  def handle_event("...", params, socket) do
    {:noreply, ...}
  end

  def render(assigns) do
    ~H"""..."""
  end
end
```

For palette_select actions that affect parent state (switch_tab, toggle_mode, push_navigate),
the component should send a message to the parent LiveView using:
```elixir
send(self(), {:command_palette_action, type, value})
```
The parent workspace_live.ex will handle {:command_palette_action, type, value} in handle_info.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CommandPaletteComponent with state and events</name>
  <files>lib/loomkin_web/live/command_palette_component.ex</files>
  <behavior>
    - Component renders nothing when command_palette_open is false
    - Component renders search input and results list when command_palette_open is true
    - handle_event("palette_search") filters agents/tabs/actions by query, updates results
    - handle_event("palette_select") sends {:command_palette_action, type, value} to parent and closes palette
    - handle_event("close_command_palette") sets command_palette_open: false
    - Results list shows "No results found" text when command_palette_results is empty
    - Component owns: command_palette_open, command_palette_query, command_palette_results
  </behavior>
  <action>
    Create lib/loomkin_web/live/command_palette_component.ex as a LiveComponent.

    Extract from workspace_live.ex:
    - The full render_command_palette/1 function body (lines ~4602-4714) → becomes render/1
    - handle_event clauses for "palette_search", "palette_select" (all type clauses), "close_command_palette"
    - The keyboard_shortcut "command_palette" clause logic (toggles open, runs initial search)

    Component state (owned by component, not parent):
    - command_palette_open: false
    - command_palette_query: ""
    - command_palette_results: []

    For update/2: accept agents assign from parent (needed for palette_search to list agent names).

    For palette_select actions that require parent navigation (switch_tab, toggle_mode,
    push_navigate to session, focus_input, refresh_channels, sub_tab change):
    send(self(), {:command_palette_action, type, value}) then close palette.
    The parent handles this message in handle_info.

    For palette_select "action" type "toggle_mode" and "switch_project": these send to parent.
    For palette_select "agent" type: send to parent for focus_card_agent.
    For palette_select "tab" type: send to parent for switch_tab.
    For palette_select "sub_tab" type: send to parent for switch_sub_tab.
    For palette_select "action/focus_input" and "action/refresh_channels": send to parent.

    Keep the phx-hook="CommandPalette" on id="command-palette" inner div (hooks stay in place).

    Run mix format after creating the file.
  </action>
  <verify>
    <automated>mix test test/loomkin_web/live/command_palette_component_test.exs --no-start 2>&1 | tail -20</automated>
  </verify>
  <done>
    CommandPaletteComponent module exists, compiles without warnings, owns its state, sends
    {:command_palette_action, type, value} to parent for navigation actions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write render tests for CommandPaletteComponent</name>
  <files>test/loomkin_web/live/command_palette_component_test.exs</files>
  <action>
    Create test/loomkin_web/live/command_palette_component_test.exs using LoomkinWeb.ConnCase
    and Phoenix.LiveViewTest.render_component/2.

    Test cases:
    1. "renders nothing when closed" — render with command_palette_open: false, assert result does not contain "command-palette"
    2. "renders search input when open" — render with command_palette_open: true, assert result contains search input placeholder text "Search agents, tabs, actions..."
    3. "shows no results message when results empty" — render with open: true, results: [], assert "No results found" in html
    4. "renders result items" — render with open: true, results: [%{type: "tab", label: "Files", value: "files", icon: nil}], assert "Files" in html

    Use render_component(LoomkinWeb.CommandPaletteComponent, %{id: "test-palette", command_palette_open: ..., command_palette_query: "", command_palette_results: [...], agents: []})

    Run mix format after creating the file.
  </action>
  <verify>
    <automated>mix test test/loomkin_web/live/command_palette_component_test.exs 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 render tests pass. mix test output shows 4 tests, 0 failures.</done>
</task>

</tasks>

<verification>
mix compile --warnings-as-errors 2>&1 | grep -i "command_palette"
mix test test/loomkin_web/live/command_palette_component_test.exs
</verification>

<success_criteria>
- lib/loomkin_web/live/command_palette_component.ex exists and compiles cleanly
- Component owns command_palette_open/query/results state
- Palette events (search, select, close) handled in component
- Navigation actions forwarded to parent via send(self(), {:command_palette_action, ...})
- All tests pass
</success_criteria>

<output>
After completion, create .planning/phases/01-monolith-extraction/01-01-SUMMARY.md
</output>
