---
phase: 09-spawn-safety
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - test/loomkin/teams/agent_spawn_gate_test.exs
  - test/loomkin_web/live/workspace_live_spawn_gate_test.exs
  - test/loomkin/teams/team_broadcaster_test.exs
autonomous: true
requirements:
  - TREE-03

must_haves:
  truths:
    - "Two new test files exist with failing stubs for all spawn gate behaviors"
    - "mix test on the two new files produces test-failures not compile errors"
    - "team_broadcaster_test.exs has new assertions for spawn gate signal types in @critical_types that currently fail"
  artifacts:
    - path: "test/loomkin/teams/agent_spawn_gate_test.exs"
      provides: "Failing stubs: budget check blocks, gate open/close, auto-approve path, timeout auto-deny"
    - path: "test/loomkin_web/live/workspace_live_spawn_gate_test.exs"
      provides: "Failing stubs: approve_spawn / deny_spawn events, toggle_auto_approve_spawns, SpawnGateRequested/Resolved signal handlers"
    - path: "test/loomkin/teams/team_broadcaster_test.exs"
      provides: "New critical_types assertions for spawn gate signal strings"
  key_links:
    - from: "test/loomkin/teams/agent_spawn_gate_test.exs"
      to: "Loomkin.Teams.Agent"
      via: "module reference in test — check_spawn_budget, get_spawn_settings"
      pattern: "check_spawn_budget"
    - from: "test/loomkin_web/live/workspace_live_spawn_gate_test.exs"
      to: "workspace_live approve_spawn / deny_spawn handlers"
      via: "phx-click event simulation"
      pattern: "approve_spawn"
---

<objective>
Create failing test stubs for all spawn safety behaviors before any implementation exists.
These tests define the contract that Plans 02-03 must satisfy.

Purpose: Nyquist compliance — every implementation task has a pre-existing failing test.
Output: Two new test files with stubbed assertions in red state, plus broadcaster test updates.
</objective>

<execution_context>
@/Users/vinnymac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vinnymac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-spawn-safety/09-CONTEXT.md
@.planning/phases/09-spawn-safety/09-RESEARCH.md

<interfaces>
<!-- Phase 6 test style reference — follow this exactly -->
<!-- test/loomkin_web/live/workspace_live_approval_test.exs: ExUnit.Case async: true, minimal Phoenix.LiveView.Socket directly, use Registry.register to simulate blocking tool task process, assert_receive for message routing -->

<!-- agent.ex spawn gate handle_call targets that tests must reference -->
<!-- GenServer.call(agent_pid, {:check_spawn_budget, estimated_cost}) -->
<!-- GenServer.call(agent_pid, :get_spawn_settings) -->
<!-- GenServer.call(agent_pid, {:set_auto_approve_spawns, enabled}) -->

<!-- workspace_live event names -->
<!-- "approve_spawn" — params: %{"gate_id" => gate_id, "agent" => agent_name} -->
<!-- "deny_spawn"    — params: %{"gate_id" => gate_id, "agent" => agent_name, "reason" => reason} -->
<!-- "toggle_auto_approve_spawns" — params: %{"agent" => agent_name, "enabled" => "true"/"false"} -->

<!-- Signal type strings for broadcaster critical_types assertions -->
<!-- "agent.spawn.gate.requested" -->
<!-- "agent.spawn.gate.resolved" -->

<!-- team_broadcaster_test.exs already tests critical_types — follow same assertion pattern for new types -->
From test/loomkin/teams/team_broadcaster_test.exs (existing pattern):
Use whatever function or attribute accessor the existing tests use for @critical_types membership checks.
Add new assertions for "agent.spawn.gate.requested" and "agent.spawn.gate.resolved".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create agent_spawn_gate_test.exs and workspace_live_spawn_gate_test.exs stubs</name>
  <files>
    test/loomkin/teams/agent_spawn_gate_test.exs,
    test/loomkin_web/live/workspace_live_spawn_gate_test.exs
  </files>
  <behavior>
    agent_spawn_gate_test.exs stubs (all must fail initially):
    - Test: {:check_spawn_budget, estimated_cost} handle_call returns {:budget_exceeded, %{remaining: _, estimated: _}} when remaining budget is below estimated cost
    - Test: {:check_spawn_budget, estimated_cost} handle_call returns :ok when remaining budget is above estimated cost
    - Test: :get_spawn_settings handle_call returns %{auto_approve_spawns: false} by default
    - Test: {:set_auto_approve_spawns, true} handle_call sets auto_approve_spawns to true and is readable via :get_spawn_settings
    - Test: spawn gate timeout auto-denies — use very short timeout (50ms in test) to keep test fast

    workspace_live_spawn_gate_test.exs stubs (all must fail initially):
    - Test: approve_spawn event routes {:spawn_gate_response, gate_id, %{outcome: :approved}} to Registry-registered blocking process
    - Test: deny_spawn event routes {:spawn_gate_response, gate_id, %{outcome: :denied, reason: reason}} to Registry-registered blocking process
    - Test: toggle_auto_approve_spawns event with enabled "true" calls set_auto_approve_spawns on agent GenServer
    - Test: handle_info for SpawnGateRequested signal sets pending_approval on the matching agent card
    - Test: handle_info for SpawnGateResolved signal clears pending_approval from the matching agent card
  </behavior>
  <action>
    Create test/loomkin/teams/agent_spawn_gate_test.exs.
    Use `ExUnit.Case, async: true`. Use @moduletag :skip at module level (Wave 0 stub pattern established in Phase 5, 7, 8).
    Reference module Loomkin.Teams.Agent — which exists. Reference handle_call keys {:check_spawn_budget, _}, :get_spawn_settings, {:set_auto_approve_spawns, _} — these do NOT exist yet, so tests will fail with match errors or function clause errors when run.
    Use `@tag :skip` on each individual test for per-test readability (established pattern).
    For timeout test, include a note about using `timeout: 50` in tool params to avoid slow test.
    Use `flunk "not implemented"` for stubs where the behavior cannot be easily described without the implementation.

    Create test/loomkin_web/live/workspace_live_spawn_gate_test.exs.
    Use `ExUnit.Case, async: true` (or `use LoomkinWeb.ConnCase` — follow whatever workspace_live_approval_test.exs uses).
    Use @moduletag :skip at module level and @tag :skip on each test.
    For Registry-routing tests: the pattern is to spawn a process that registers under {:spawn_gate, gate_id} in Loomkin.Teams.AgentRegistry, then simulate the LiveView event, then use assert_receive to verify the message was routed.
    Use `flunk "not implemented"` for stubs where Registry simulation is complex.

    Do NOT attempt to make tests pass. Goal: files compile, tests run (or are skipped), no syntax errors.
    Run `mix format` after creating both files.
  </action>
  <verify>
    <automated>cd /Users/vinnymac/Sites/vinnymac/loomkin && mix test test/loomkin/teams/agent_spawn_gate_test.exs test/loomkin_web/live/workspace_live_spawn_gate_test.exs --no-start 2>&1 | tail -20</automated>
  </verify>
  <done>Both files compile without syntax errors; tests are skipped (Wave 0 pattern) or run and show failures — no compile errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add spawn gate critical_types assertions to team_broadcaster_test.exs</name>
  <files>
    test/loomkin/teams/team_broadcaster_test.exs
  </files>
  <behavior>
    team_broadcaster_test.exs additions:
    - New test: TeamBroadcaster classifies "agent.spawn.gate.requested" as critical (currently fails — type not in @critical_types yet)
    - New test: TeamBroadcaster classifies "agent.spawn.gate.resolved" as critical (currently fails)
    - Existing passing tests must remain green
  </behavior>
  <action>
    Read test/loomkin/teams/team_broadcaster_test.exs fully before editing.
    Find the section where critical_types are tested (approval types were added in Phase 6 — follow that exact pattern).
    Add two new test assertions for the spawn gate signal type strings.
    These tests fail until Plan 02 adds the types to @critical_types.

    Run `mix format` after editing.
  </action>
  <verify>
    <automated>cd /Users/vinnymac/Sites/vinnymac/loomkin && mix test test/loomkin/teams/team_broadcaster_test.exs --no-start 2>&1 | tail -20</automated>
  </verify>
  <done>
    New spawn gate assertions fail (types not yet in @critical_types). All previously passing tests remain green.
  </done>
</task>

</tasks>

<verification>
mix test test/loomkin/teams/agent_spawn_gate_test.exs test/loomkin_web/live/workspace_live_spawn_gate_test.exs test/loomkin/teams/team_broadcaster_test.exs --no-start

Expected: all three files compile cleanly, new spawn gate assertions in broadcaster test fail, new stub files produce skips or failures (not compile errors), pre-existing passing tests remain green.
</verification>

<success_criteria>
- test/loomkin/teams/agent_spawn_gate_test.exs exists and compiles
- test/loomkin_web/live/workspace_live_spawn_gate_test.exs exists and compiles
- team_broadcaster_test.exs has two new failing assertions for spawn gate critical types
- No regressions in existing broadcaster tests
- No watch-mode flags used
</success_criteria>

<output>
After completion, create `.planning/phases/09-spawn-safety/09-01-SUMMARY.md`
</output>
