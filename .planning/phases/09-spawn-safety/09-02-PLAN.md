---
phase: 09-spawn-safety
plan: "02"
type: execute
wave: 1
depends_on:
  - "09-01"
files_modified:
  - lib/loomkin/signals/spawn.ex
  - lib/loomkin/teams/agent.ex
  - lib/loomkin/teams/team_broadcaster.ex
autonomous: true
requirements:
  - TREE-03

must_haves:
  truths:
    - "SpawnGateRequested and SpawnGateResolved signal structs exist and publish successfully"
    - "Agent on_tool_execute intercept for team_spawn runs budget check before spawning"
    - "Budget exceeded path returns structured tool error without opening a gate"
    - "Auto-approve path skips the gate and proceeds directly to spawn when within budget"
    - "Human gate path blocks tool task on receive, publishes SpawnGateRequested, resolves on response"
    - "Spawn gate timeout auto-denies with structured reason after configurable timeout"
    - "Agent state has auto_approve_spawns field and handle_call accessors for budget check and settings"
    - "SpawnGateRequested and SpawnGateResolved classified as critical in TeamBroadcaster"
  artifacts:
    - path: "lib/loomkin/signals/spawn.ex"
      provides: "Loomkin.Signals.Spawn.GateRequested and GateResolved signal structs"
      contains: "agent.spawn.gate.requested, agent.spawn.gate.resolved"
    - path: "lib/loomkin/teams/agent.ex"
      provides: "auto_approve_spawns field, budget check handle_call, spawn gate intercept in on_tool_execute"
    - path: "lib/loomkin/teams/team_broadcaster.ex"
      provides: "@critical_types updated with spawn gate signal type strings"
  key_links:
    - from: "lib/loomkin/teams/agent.ex"
      to: "lib/loomkin/signals/spawn.ex"
      via: "Loomkin.Signals.Spawn.GateRequested.new! in on_tool_execute intercept"
      pattern: "GateRequested"
    - from: "lib/loomkin/teams/agent.ex"
      to: "Loomkin.Teams.CostTracker"
      via: "CostTracker.team_cost_summary/1 in handle_call(:check_spawn_budget)"
      pattern: "team_cost_summary"
    - from: "lib/loomkin/teams/agent.ex"
      to: "Loomkin.Teams.AgentRegistry"
      via: "Registry.register({:spawn_gate, gate_id}, self())"
      pattern: "spawn_gate"
---

<objective>
Implement the spawn safety backend: new signal structs, agent.ex spawn gate intercept with budget check
and auto-approve support, and TeamBroadcaster critical classification.

Purpose: Establishes the full server-side contract for spawn gating — the intercept pre-empts TeamSpawn
execution, budget check blocks over-budget spawns silently, the gate blocks only the tool task process,
and spawn signals are delivered instantly to LiveView via the critical bypass.
Output: Fully tested backend that Plan 03 wires into workspace_live event handlers.
</objective>

<execution_context>
@/Users/vinnymac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vinnymac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-spawn-safety/09-CONTEXT.md
@.planning/phases/09-spawn-safety/09-RESEARCH.md
@.planning/phases/09-spawn-safety/09-01-SUMMARY.md

<interfaces>
<!-- Canonical signal struct pattern — mirrors lib/loomkin/signals/approval.ex exactly -->
From lib/loomkin/signals/approval.ex:
```elixir
defmodule Loomkin.Signals.Approval.Requested do
  use Jido.Signal,
    type: "agent.approval.requested",
    schema: [
      gate_id:    [type: :string, required: true],
      agent_name: [type: :string, required: true],
      team_id:    [type: :string, required: true],
      question:   [type: :string, required: true],
      timeout_ms: [type: :integer, required: false]
    ]
end
```

<!-- Existing on_tool_execute team_spawn branch in agent.ex — Phase 9 REPLACES the if-block with a full pre-spawn intercept -->
From lib/loomkin/teams/agent.ex (lines 2169-2178 approximately):
```elixir
if tool_module == Loomkin.Tools.TeamSpawn do
  case result do
    {:ok, %{team_id: child_team_id}} ->
      send(agent_pid, {:child_team_spawned, child_team_id})
    _ ->
      :ok
  end
end
```
Phase 9 expands this block so the TeamSpawn branch runs BEFORE default_run_tool rather than only after.
The post-success child_team_spawned notify is preserved inside the approved path.

<!-- CostTracker.team_cost_summary/1 — returns {total_cost_usd, total_tokens, task_count} -->
<!-- total_cost_usd may be a Decimal struct — use Decimal.to_float/1 or Float.parse to normalize -->
From lib/loomkin/teams/cost_tracker.ex:
```elixir
def team_cost_summary(team_id) do
  # returns {total_cost_usd, total_tokens, task_count}
end
```

<!-- Budget limit: workspace_live uses roster_budget/1 which defaults limit to 5.0 when no config -->
<!-- Agent handle_call for budget check should default to 5.0 if no budget_limit in state -->

<!-- TeamBroadcaster @critical_types — add two spawn gate type strings -->
From lib/loomkin/teams/team_broadcaster.ex:
```elixir
@critical_types MapSet.new([
  # ... existing types including approval types added in Phase 6 ...
  # ADD: "agent.spawn.gate.requested"
  # ADD: "agent.spawn.gate.resolved"
])
```

<!-- Per-role cost estimates — hardcoded constants, Claude's discretion for values -->
@role_cost_estimates %{
  "researcher" => 0.20,
  "coder"      => 0.50,
  "reviewer"   => 0.30,
  "tester"     => 0.30,
  "lead"       => 0.50,
  "concierge"  => 0.10,
  "orienter"   => 0.10
}
<!-- Default for unknown role: 0.20 -->

<!-- Pitfall: do NOT read auto_approve_spawns from captured state in closure -->
<!-- MUST use GenServer.call(agent_pid, :get_spawn_settings) to get fresh value -->

<!-- Pitfall: guard against double-gate collision -->
<!-- If agent status is :approval_pending when spawn is attempted, return immediate tool error -->
<!-- rather than trying to open a second gate on top of existing one -->

<!-- existing defstruct location in agent.ex — add auto_approve_spawns: false near spawned_child_teams field -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create spawn signal structs and add auto_approve_spawns to agent defstruct</name>
  <files>
    lib/loomkin/signals/spawn.ex,
    lib/loomkin/teams/agent.ex
  </files>
  <behavior>
    lib/loomkin/signals/spawn.ex:
    - Loomkin.Signals.Spawn.GateRequested: type "agent.spawn.gate.requested", schema: gate_id (string, required), agent_name (string, required), team_id (string, required), team_name (string, required), roles (list of maps, required), estimated_cost (float, required), limit_warning (atom, optional — :depth | :agents | nil), timeout_ms (integer, optional), auto_approve_spawns (boolean, optional — current agent setting to render checkbox state)
    - Loomkin.Signals.Spawn.GateResolved: type "agent.spawn.gate.resolved", schema: gate_id (string, required), agent_name (string, required), team_id (string, required), outcome (atom, required — :approved | :denied | :timeout)

    lib/loomkin/teams/agent.ex defstruct additions:
    - auto_approve_spawns: false  (session-scoped boolean, never persisted)

    lib/loomkin/teams/agent.ex new handle_call clauses:
    - handle_call(:get_spawn_settings, _from, state) -> reply with %{auto_approve_spawns: state.auto_approve_spawns}
    - handle_call({:set_auto_approve_spawns, enabled}, _from, state) -> update auto_approve_spawns in state, reply :ok
    - handle_call({:check_spawn_budget, estimated_cost}, _from, state) -> query CostTracker.team_cost_summary(state.team_id), extract total_cost_usd as float, compare (budget_limit - spent) < estimated_cost, reply {:budget_exceeded, %{remaining: remaining, estimated: estimated_cost}} or :ok. Budget limit defaults to 5.0 if not set in state.
    - handle_call({:open_spawn_gate, gate_id, pending_info}, _from, state) -> set status to :approval_pending via set_status_and_broadcast, store pending_info in a spawn_gate_pending field (or reuse existing mechanism), reply :ok
  </behavior>
  <action>
    Create lib/loomkin/signals/spawn.ex with two nested signal modules: Loomkin.Signals.Spawn.GateRequested
    and Loomkin.Signals.Spawn.GateResolved. Model directly on lib/loomkin/signals/approval.ex — same
    `use Jido.Signal, type:, schema:` pattern.

    Read lib/loomkin/teams/agent.ex fully before editing (it is large — ~2200+ lines).
    Locate the defstruct at the top and add `auto_approve_spawns: false` near `spawned_child_teams`.
    Find handle_call/3 clauses and add the three new clauses: :get_spawn_settings, :set_auto_approve_spawns,
    {:check_spawn_budget, estimated_cost}. The budget check handle_call queries CostTracker synchronously
    (safe: called from tool task via GenServer.call, not from GenServer itself).

    For {:check_spawn_budget, _}, use the same Decimal.to_float pattern workspace_live uses in roster_budget/1
    to normalize the CostTracker total_cost_usd value. Default budget_limit to 5.0.

    Do NOT add the on_tool_execute intercept yet — that is Task 2.
    Run `mix format` after all edits.
  </action>
  <verify>
    <automated>cd /Users/vinnymac/Sites/vinnymac/loomkin && mix test test/loomkin/teams/agent_spawn_gate_test.exs --no-start 2>&1 | tail -20</automated>
  </verify>
  <done>
    Loomkin.Signals.Spawn.GateRequested and GateResolved compile and new!/1 works.
    agent_spawn_gate_test.exs tests for :get_spawn_settings, :set_auto_approve_spawns, and :check_spawn_budget pass (or become un-skipped and pass).
    mix compile produces no errors or warnings for the modified files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement spawn gate intercept in agent on_tool_execute and update TeamBroadcaster</name>
  <files>
    lib/loomkin/teams/agent.ex,
    lib/loomkin/teams/team_broadcaster.ex
  </files>
  <behavior>
    agent.ex on_tool_execute TeamSpawn branch:
    - Pre-spawn intercept runs BEFORE AgentLoop.default_run_tool/3 for tool_module == Loomkin.Tools.TeamSpawn
    - Step 1: compute estimated_cost from tool_args roles using @role_cost_estimates module attribute
    - Step 2: guard against double-gate — if agent status is :approval_pending, return immediate {:error, :approval_pending, %{message: "Agent already has an open approval gate"}} — no second gate
    - Step 3: GenServer.call(agent_pid, {:check_spawn_budget, estimated_cost}) — if {:budget_exceeded, details}, return AgentLoop.format_tool_result({:error, :budget_exceeded, details})
    - Step 4: GenServer.call(agent_pid, :get_spawn_settings) — if auto_approve_spawns is true and budget ok, skip gate entirely and go to step 6
    - Step 5 (human gate): compute gate_id = Ecto.UUID.generate(), build pending_info map with type: :spawn_gate, compute limit_warning (:depth or :agents or nil at 80% of Manager limits), call GenServer.call(agent_pid, {:open_spawn_gate, gate_id, pending_info}), Registry.register AgentRegistry under {:spawn_gate, gate_id}, publish GateRequested signal, block on receive with 300_000ms timeout (configurable via tool_args["timeout_ms"] or Application.get_env(:loomkin, :spawn_gate_timeout_ms, 300_000))
    - Step 5a: receive {:spawn_gate_response, ^gate_id, %{outcome: :approved}} -> Registry.unregister, proceed to step 6
    - Step 5b: receive {:spawn_gate_response, ^gate_id, %{outcome: :denied, reason: reason}} -> Registry.unregister, publish GateResolved(outcome: :denied), return {:ok, %{status: :denied, reason: :human_denied, message: reason || "Denied by human."}}
    - Step 5c: after timeout -> Registry.unregister, publish GateResolved(outcome: :timeout), return {:ok, %{status: :denied, reason: :timeout, message: "Spawn gate timed out."}}
    - Step 6 (execute): result = AgentLoop.default_run_tool(tool_module, tool_args, context), if approved publish GateResolved(outcome: :approved), preserve existing child_team_spawned notify on success

    Private helpers in agent.ex:
    - estimate_spawn_cost(roles) — uses @role_cost_estimates, defaults unknown role to 0.20
    - build_spawn_pending_info(gate_id, tool_args, estimated_cost, limit_warning, agent_name, team_id, auto_approve_spawns) — builds the pending_approval map
    - compute_limit_warning(team_id, planned_agent_count) — checks 80% of max depth/agents, returns :depth | :agents | nil

    TeamBroadcaster:
    - Add "agent.spawn.gate.requested" to @critical_types MapSet
    - Add "agent.spawn.gate.resolved" to @critical_types MapSet
  </behavior>
  <action>
    Read lib/loomkin/teams/agent.ex to find the existing on_tool_execute closure body, specifically the
    `if tool_module == Loomkin.Tools.TeamSpawn do` block. The block currently only runs post-success.
    Restructure it so that for TeamSpawn: the pre-spawn intercept runs first (steps 1-5), and the
    default_run_tool call happens only if approved or auto-approved (step 6).
    The existing `{:ok, %{team_id: child_team_id}} -> send(agent_pid, {:child_team_spawned, child_team_id})`
    logic is preserved inside the step 6 success path.

    CRITICAL: The receive block must execute in the tool task process (the Task spawned by AgentLoop),
    NOT in the agent GenServer. The closure is called from the tool task. Do NOT move this into a handle_call.
    This is the same pattern as RequestApproval.run/2 and AskUser.run/2.

    CRITICAL: Read auto_approve_spawns via GenServer.call(agent_pid, :get_spawn_settings) inside the
    closure, not from captured state. The closure captures state at loop-build time; the user may toggle
    auto-approve between spawns.

    Add @role_cost_estimates as a module attribute near other module-level constants.
    Add private helper functions estimate_spawn_cost/1 and build_spawn_pending_info/7 near other private
    agent helpers.
    Add compute_limit_warning/2 — for depth warning, check if parent team nesting depth + 1 >= 80% of
    Manager.@default_max_nesting_depth (which is 2). Since 2 is the max and 1 is 80% of 2 rounded down,
    effectively this triggers on any sub-team spawn (depth 1 -> spawning depth 2 = at 100% limit). Use
    this to decide :depth warning. For agent count: query team agent count via Registry or Manager if
    available; if no clean query path, compute from tool_args roles length vs. a @default_max_agents_per_team 10
    constant defined in agent.ex (mirroring Manager's pattern). Emit :agents warning if planned spawn would
    bring total to >= 80% of the limit.

    Read lib/loomkin/teams/team_broadcaster.ex. Find @critical_types. Add both spawn gate type strings.

    Run `mix format` after all edits.
  </action>
  <verify>
    <automated>cd /Users/vinnymac/Sites/vinnymac/loomkin && mix test test/loomkin/teams/agent_spawn_gate_test.exs test/loomkin/teams/team_broadcaster_test.exs --no-start 2>&1 | tail -20</automated>
  </verify>
  <done>
    agent_spawn_gate_test.exs tests for budget check, gate intercept, auto-approve path, and timeout pass (or un-skip and pass).
    team_broadcaster_test.exs new spawn gate critical_types assertions pass.
    mix test (full suite) has no regressions.
  </done>
</task>

</tasks>

<verification>
mix test test/loomkin/teams/agent_spawn_gate_test.exs test/loomkin/teams/team_broadcaster_test.exs --no-start

Then: mix test (full suite, no regressions)
</verification>

<success_criteria>
- Loomkin.Signals.Spawn.GateRequested and GateResolved exist with correct type strings
- agent.ex has auto_approve_spawns field and three new handle_call clauses
- agent.ex on_tool_execute intercept for TeamSpawn runs pre-spawn with budget check, gate blocking, auto-approve, timeout
- "agent.spawn.gate.requested" and "agent.spawn.gate.resolved" in TeamBroadcaster @critical_types
- Full mix test suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/09-spawn-safety/09-02-SUMMARY.md`
</output>
