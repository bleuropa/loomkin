---
phase: 06-approval-gates
plan: "03"
type: execute
wave: 2
depends_on:
  - "06-02"
files_modified:
  - lib/loomkin_web/live/workspace_live.ex
autonomous: true
requirements:
  - INTV-02

must_haves:
  truths:
    - "Clicking Approve button routes approval response to blocking RequestApproval tool task via Registry"
    - "Clicking Deny button routes denial with reason to blocking tool task via Registry"
    - "ApprovalRequested signal sets pending_approval map on the correct agent card assign"
    - "ApprovalResolved signal clears pending_approval from card and clears leader banner"
    - "Leader banner assign is set when agent with role :lead hits approval gate"
    - "Approval gate open and close events appear in comms feed with purple styling"
  artifacts:
    - path: "lib/loomkin_web/live/workspace_live.ex"
      provides: "handle_event approve_card_agent, deny_card_agent, handle_info ApprovalRequested/Resolved, leader banner assign, comms feed events"
      exports: ["handle_event/3", "handle_info/2"]
  key_links:
    - from: "lib/loomkin_web/live/workspace_live.ex"
      to: "Loomkin.Teams.AgentRegistry"
      via: "send_approval_response/2 via Registry.lookup"
      pattern: "approval_gate"
    - from: "lib/loomkin_web/live/workspace_live.ex"
      to: "socket.assigns.cards[agent_name][:role]"
      via: "leader banner toggle in ApprovalRequested handler"
      pattern: "leader_approval_pending"
---

<objective>
Wire the approval gate into workspace_live.ex: event handlers for Approve/Deny buttons,
handle_info clauses for approval signals, leader banner assign management, and comms feed events.

Purpose: Connects the UI actions (Plans 04) to the backend tool task (Plan 02) via Registry routing.
Without this plan, clicking Approve does nothing and signals never reach the card component.
Output: Fully tested workspace_live server-side plumbing for approval gates.
</objective>

<execution_context>
@/Users/vinnymac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vinnymac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-approval-gates/06-CONTEXT.md
@.planning/phases/06-approval-gates/06-RESEARCH.md
@.planning/phases/06-approval-gates/06-02-SUMMARY.md

<interfaces>
<!-- Canonical response routing pattern — approval mirrors this -->
From lib/loomkin_web/live/workspace_live.ex:
```elixir
defp send_ask_user_answer(question_id, answer) do
  case Registry.lookup(Loomkin.Teams.AgentRegistry, {:ask_user, question_id}) do
    [{pid, _}] -> send(pid, {:ask_user_answer, question_id, answer})
    [] -> :ok
  end
end

def handle_event("ask_user_answer", %{"question-id" => question_id, "answer" => answer}, socket) do
  send_ask_user_answer(question_id, answer)
  {:noreply, socket}
end
```

<!-- push_activity_event pattern — follow for comms feed events -->
From lib/loomkin_web/live/workspace_live.ex:
```elixir
defp push_activity_event(socket, event) do
  # streams to comms feed; event is a map with :type, :agent_name, :content, etc.
  ...
end
```

<!-- Leader banner needs: agent role lookup from cards assigns -->
```elixir
# socket.assigns.cards is a map of agent_name -> card_map
# card_map has :role field
# When ApprovalRequested signal arrives, look up role:
role = get_in(socket.assigns, [:cards, agent_name, :role])
leader_approval_pending = if role == :lead, do: %{gate_id: ..., timeout_ms: ..., started_at: ...}, else: nil
```

<!-- New signal types from Plan 02 -->
# Loomkin.Signals.Approval.Requested — type: "agent.approval.requested"
# Loomkin.Signals.Approval.Resolved — type: "agent.approval.resolved"
# Signal data fields: gate_id, agent_name, team_id, question, timeout_ms (Requested); gate_id, agent_name, team_id, outcome (Resolved)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add approve/deny handle_event and send_approval_response helper</name>
  <files>lib/loomkin_web/live/workspace_live.ex</files>
  <behavior>
    - handle_event("approve_card_agent", %{"gate-id" => gate_id, "agent" => agent_name, "context" => ctx}, socket)
      -> calls send_approval_response(gate_id, %{outcome: :approved, context: ctx || nil})
      -> clears pending_approval from that agent's card in assigns
      -> returns {:noreply, socket}
    - handle_event("deny_card_agent", %{"gate-id" => gate_id, "agent" => agent_name, "reason" => reason}, socket)
      -> calls send_approval_response(gate_id, %{outcome: :denied, reason: reason || nil, context: nil})
      -> clears pending_approval from that agent's card in assigns
      -> clears @leader_approval_pending assign if gate_id matches
      -> returns {:noreply, socket}
    - defp send_approval_response(gate_id, decision):
      -> Registry.lookup(Loomkin.Teams.AgentRegistry, {:approval_gate, gate_id})
      -> if [{pid, _}] -> send(pid, {:approval_response, gate_id, decision})
      -> if [] -> :ok
  </behavior>
  <action>
    Read lib/loomkin_web/live/workspace_live.ex to understand the handle_event clustering pattern —
    add new clauses near the other agent-interaction events (ask_user_answer, permission_response).

    Add the two handle_event clauses. The "context" param may be an empty string from a simple button click —
    normalize empty string to nil before passing as context.

    Add defp send_approval_response/2 near defp send_ask_user_answer/2 for consistency.

    For clearing pending_approval from the card: update socket.assigns.cards[agent_name] by removing
    the :pending_approval key. Follow the same pattern used for other card assign updates (look at how
    crash/recovery clears card fields — likely via an update_card helper or direct assigns update).

    Run `mix format` after edits.
  </action>
  <verify>
    <automated>cd /Users/vinnymac/Sites/vinnymac/loomkin && mix test test/loomkin_web/live/workspace_live_approval_test.exs --no-start 2>&1 | grep -E "approve_card|deny_card|send_approval|test" | head -20</automated>
  </verify>
  <done>
    workspace_live_approval_test.exs tests for approve_card_agent and deny_card_agent pass.
    mix compile with no errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add handle_info for ApprovalRequested/Resolved, leader banner, comms feed events</name>
  <files>lib/loomkin_web/live/workspace_live.ex</files>
  <behavior>
    handle_info for ApprovalRequested signal (type "agent.approval.requested"):
    - Extract gate_id, agent_name, team_id, question, timeout_ms from signal data
    - Update the agent's card in assigns with pending_approval: %{gate_id: gate_id, question: question, timeout_ms: timeout_ms, started_at: System.monotonic_time(:millisecond)}
    - Look up role from socket.assigns.cards[agent_name][:role]
    - If role == :lead, set @leader_approval_pending assign to %{gate_id: gate_id, timeout_ms: timeout_ms, started_at: started_at}
    - Push approval_gate_requested comms event via push_activity_event: type :approval_gate_requested, content "Agent X is requesting approval: [question]", agent_name
    - Return {:noreply, socket}

    handle_info for ApprovalResolved signal (type "agent.approval.resolved"):
    - Extract gate_id, agent_name, team_id, outcome from signal data
    - Clear pending_approval from that agent's card assigns
    - If @leader_approval_pending[:gate_id] == gate_id, set @leader_approval_pending to nil
    - Push approval_gate_resolved comms event: type :approval_gate_resolved, content "Approval gate [outcome] by human" (or "Approval gate timed out" for :timeout)
    - Return {:noreply, socket}

    Note: Both handle_event (user action) AND handle_info (timeout) must clear card state.
    This handles the case where the tool times out and sends ApprovalResolved without user interaction.
  </behavior>
  <action>
    Read the existing handle_info cluster in workspace_live.ex. Add the two new clauses near the
    permission signal handlers (they share the same concern — agent intervention state changes).

    Signal pattern match: `%Jido.Signal{type: "agent.approval.requested"} = sig` — data is in `sig.data`.

    For card assign update: use the same helper pattern already used for updating agent cards (crash signals,
    permission signals). The card map stored in assigns needs pending_approval merged in or removed.

    For the leader banner: assign key is :leader_approval_pending (nil when no gate active).

    For comms events: follow push_activity_event pattern. Use event types :approval_gate_requested and
    :approval_gate_resolved — these types must match what Plan 04 adds to AgentCommsComponent @type_config.

    Run `mix format` after edits.
  </action>
  <verify>
    <automated>cd /Users/vinnymac/Sites/vinnymac/loomkin && mix test test/loomkin_web/live/workspace_live_approval_test.exs --no-start 2>&1 | tail -20</automated>
  </verify>
  <done>
    All workspace_live_approval_test.exs tests pass (leader banner set/clear, card pending_approval set/clear,
    comms events pushed).
    Full suite: mix test — no regressions.
  </done>
</task>

</tasks>

<verification>
mix test test/loomkin_web/live/workspace_live_approval_test.exs --no-start

Then: mix test (full suite, no regressions)
</verification>

<success_criteria>
- handle_event "approve_card_agent" and "deny_card_agent" exist and route to Registry
- send_approval_response/2 helper mirrors send_ask_user_answer/2 pattern
- handle_info for both approval signal types exists
- Leader banner assign (@leader_approval_pending) toggled correctly by role == :lead
- Comms feed events pushed for gate open and close
- All workspace_live_approval_test.exs tests pass
- Full mix test suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/06-approval-gates/06-03-SUMMARY.md`
</output>
