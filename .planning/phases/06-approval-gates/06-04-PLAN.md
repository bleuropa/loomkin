---
phase: 06-approval-gates
plan: "04"
type: execute
wave: 2
depends_on:
  - "06-02"
files_modified:
  - lib/loomkin_web/live/agent_card_component.ex
  - lib/loomkin_web/live/agent_comms_component.ex
  - assets/js/app.js
autonomous: false
requirements:
  - INTV-02

must_haves:
  truths:
    - "Agent card shows purple expanded panel with question text and three buttons when status is :approval_pending and pending_approval assign is present"
    - "Approve button is visible and emits approve_card_agent event; Deny button emits deny_card_agent event"
    - "Approve w/ Context reveals a textarea; Deny reveals a textarea — both hidden by default"
    - "Status dot for :approval_pending is violet (bg-violet-500 animate-pulse), not amber"
    - "card_state_class for :approval_pending uses 'agent-card-approval' CSS class"
    - "AgentCommsComponent renders approval_gate_requested and approval_gate_resolved events with purple styling"
    - "CountdownTimer JS hook ticks down and clears its interval on destroyed()"
    - "Approval card is visually distinct from permission hook card (purple vs amber)"
  artifacts:
    - path: "lib/loomkin_web/live/agent_card_component.ex"
      provides: "Expanded approval panel, purple card state class, violet status dot, three-button layout, countdown hook"
    - path: "lib/loomkin_web/live/agent_comms_component.ex"
      provides: "approval_gate_requested and approval_gate_resolved entries in @type_config"
    - path: "assets/js/app.js"
      provides: "CountdownTimer JS hook with setInterval + destroyed() cleanup"
  key_links:
    - from: "lib/loomkin_web/live/agent_card_component.ex"
      to: "workspace_live approve_card_agent"
      via: "phx-click on Approve button"
      pattern: "approve_card_agent"
    - from: "assets/js/app.js"
      to: "CountdownTimer hook element"
      via: "phx-hook=\"CountdownTimer\" data-deadline-at"
      pattern: "CountdownTimer"
---

<objective>
Implement the approval gate UI: expanded purple card panel with three-button layout and countdown timer,
violet status dot, approval-specific CSS class, comms feed event styling, and CountdownTimer JS hook.

Purpose: Makes approval gates visible and actionable in the UI — the human sees the purple panel,
reads the question, and acts with Approve / Deny / Approve w/ Context.
Output: Complete approval gate visual surface with passing component tests and visual verification.
</objective>

<execution_context>
@/Users/vinnymac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vinnymac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-approval-gates/06-CONTEXT.md
@.planning/phases/06-approval-gates/06-RESEARCH.md
@.planning/phases/06-approval-gates/06-02-SUMMARY.md

<interfaces>
<!-- AgentCardComponent existing approval_pending hooks — update these -->
From lib/loomkin_web/live/agent_card_component.ex:
```elixir
defp card_state_class(_content_type, :approval_pending), do: "agent-card-blocked"  # -> "agent-card-approval"
defp status_dot_class(:approval_pending), do: "bg-amber-400 agent-dot-thinking"     # -> "bg-violet-500 animate-pulse"
defp status_label(:approval_pending), do: "Awaiting approval"                       # keep as-is
```

<!-- Comms feed @type_config structure to extend -->
From lib/loomkin_web/live/agent_comms_component.ex @type_config:
```elixir
# Add these two entries (use violet/purple palette consistent with decision type):
approval_gate_requested: %{
  icon: "🔐",
  accent_border: "rgba(124, 58, 237, 0.40)",
  accent_text: "#a78bfa",
  accent_bg: "rgba(124, 58, 237, 0.12)"
},
approval_gate_resolved: %{
  icon: "✔",
  accent_border: "rgba(124, 58, 237, 0.30)",
  accent_text: "#8b5cf6",
  accent_bg: "rgba(124, 58, 237, 0.08)"
}
```

<!-- Existing card structure — approval panel appended as bottom extension -->
# @card.status == :approval_pending AND @card[:pending_approval] must both be truthy
# @card[:pending_approval] map: %{gate_id, question, timeout_ms, started_at}
# started_at is System.system_time(:millisecond) (wall clock — compatible with JS Date.now())
# deadline_at for JS = started_at + timeout_ms
# @card.name used for JS element IDs and phx-value-agent

<!-- Existing JS hook pattern from assets/js/app.js -->
# CommsFeedScroll uses mounted() + updated() + destroyed() callbacks
# CountdownTimer: data-deadline-at is wall-clock ms timestamp
# remaining = Math.max(0, Math.ceil((deadline - Date.now()) / 1000))
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update AgentCardComponent — approval panel, violet dot, card class, comms config</name>
  <files>
    lib/loomkin_web/live/agent_card_component.ex,
    lib/loomkin_web/live/agent_comms_component.ex
  </files>
  <behavior>
    agent_card_component.ex:
    - card_state_class(:approval_pending) returns "agent-card-approval" (not "agent-card-blocked")
    - status_dot_class(:approval_pending) returns "bg-violet-500 animate-pulse" (not amber)
    - Approval panel renders when @card.status == :approval_pending AND @card[:pending_approval] is present
    - Panel: purple border strip, "Approval required" header text (violet-400), countdown span (phx-hook="CountdownTimer", data-deadline-at computed as started_at + timeout_ms)
    - Panel: question text rendered from @card[:pending_approval][:question]
    - Three buttons in a row: Approve (violet), Approve w/ Context (lighter violet), Deny (rose)
    - Approve button: phx-click="approve_card_agent", phx-value-gate-id, phx-value-agent, phx-value-context=""
    - Approve w/ Context: JS.toggle to show approve-ctx-{agent} div; that div has a form with phx-submit="approve_card_agent", textarea name="context", hidden inputs for gate-id and agent
    - Deny: JS.toggle to show deny-ctx-{agent} div; that div has a form with phx-submit="deny_card_agent", textarea name="reason", hidden inputs for gate-id and agent

    agent_comms_component.ex:
    - Add approval_gate_requested entry to @type_config with purple styling
    - Add approval_gate_resolved entry to @type_config with lighter purple styling
  </behavior>
  <action>
    Read lib/loomkin_web/live/agent_card_component.ex fully. Note how the permission hook
    (waiting_permission) section renders inline — the approval panel is a similar conditional block
    but appended below the card content area (NOT absolute overlay).

    The approval panel should appear below the main card content when @card.status == :approval_pending.
    Use :if to conditionally render. The panel uses Tailwind classes with purple/violet palette.

    For the countdown: use data-deadline-at attribute computed in the template:
    {@card[:pending_approval][:started_at] + @card[:pending_approval][:timeout_ms]}

    For Approve w/ Context and Deny expanding textareas: use Phoenix.LiveView.JS.toggle(to: "#id").
    The forms use phx-submit to send the event to workspace_live.

    Read lib/loomkin_web/live/agent_comms_component.ex fully. Add the two new entries to @type_config map.
    Keys must be atoms matching the event types pushed by workspace_live in Plan 03.

    Run `mix format` after all edits.
  </action>
  <verify>
    <automated>cd /Users/vinnymac/Sites/vinnymac/loomkin && mix test test/loomkin_web/live/agent_card_component_test.exs --no-start 2>&1 | tail -20</automated>
  </verify>
  <done>
    All agent_card_component_test.exs tests pass — including:
    - :approval_pending dot is violet (updated assertion)
    - card_state_class uses "agent-card-approval"
    - Approval panel renders with three buttons when pending_approval assign present
    mix compile — no errors or warnings on modified files
  </done>
</task>

<task type="auto">
  <name>Task 2: CountdownTimer JS hook in app.js</name>
  <files>assets/js/app.js</files>
  <action>
    Read assets/js/app.js. Find where JS hooks are defined (look for the Hooks object and CommsFeedScroll).

    Add CountdownTimer hook:
    ```javascript
    CountdownTimer: {
      mounted() {
        this.tick();
        this.intervalId = setInterval(() => this.tick(), 1000);
      },
      tick() {
        const deadline = parseInt(this.el.dataset.deadlineAt, 10);
        const remaining = Math.max(0, Math.ceil((deadline - Date.now()) / 1000));
        const minutes = Math.floor(remaining / 60);
        const seconds = remaining % 60;
        this.el.textContent = `${minutes}:${seconds.toString().padStart(2, '0')}`;
        if (remaining === 0) {
          clearInterval(this.intervalId);
        }
      },
      destroyed() {
        clearInterval(this.intervalId);
      }
    }
    ```

    Register it in the LiveSocket hooks object alongside CommsFeedScroll and any other existing hooks.
    Do NOT break existing hooks. `data-deadline-at` is a wall-clock millisecond timestamp
    (System.system_time(:millisecond) + timeout_ms), so `Date.now()` subtraction is correct.

    Run `mix format` (Elixir only; JS formatting is not required by project conventions).
  </action>
  <verify>
    <automated>cd /Users/vinnymac/Sites/vinnymac/loomkin && mix assets.build 2>&1 | tail -10</automated>
  </verify>
  <done>
    assets/js/app.js compiles without JS errors (mix assets.build succeeds).
    CountdownTimer hook is registered in the LiveSocket hooks object.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — approval gate UI distinct from permission hook</name>
  <action>
    Before presenting to user: run `mix test` to confirm full suite is green and `mix assets.build` confirms
    no JS compilation errors. Collect output to show alongside the verification instructions.
  </action>
  <verify>
    <automated>cd /Users/vinnymac/Sites/vinnymac/loomkin && mix test 2>&1 | tail -5 && mix assets.build 2>&1 | tail -5</automated>
  </verify>
  <done>Human confirms approval gate UI is visually distinct from the permission hook and all three button actions work correctly</done>
  <what-built>
    Complete approval gate UI:
    - Purple expanded card panel with question, three-button layout (Approve / Deny / Approve w/ Context)
    - Violet status dot and "agent-card-approval" card class (distinct from amber permission hook)
    - CountdownTimer JS hook ticking from deadline
    - Leader banner above agent grid (when leader role hits gate)
    - Comms feed events with purple styling for gate open and close
  </what-built>
  <how-to-verify>
    1. Start the dev server: `mix phx.server` in loomkin directory
    2. Open http://loom.test:4200 and start a multi-agent team session with a leader agent
    3. Trigger a RequestApproval call from an agent (or use IEx to simulate the signal)
    4. Verify: agent card expands with purple border, shows question text, countdown timer ticking
    5. Verify: "Awaiting approval" label with violet dot (not amber)
    6. Verify: purple card is visually distinct from any amber permission hook card
    7. Trigger from the leader agent specifically — verify team-wide banner appears above the grid
    8. Click "Approve" — verify card collapses, comms feed shows purple resolution event
    9. Trigger again, click "Deny" — verify tool receives denial reason
    10. Trigger again, let timer run out — verify auto-deny and timeout reason in comms feed
  </how-to-verify>
  <resume-signal>Type "approved" to proceed or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
mix test test/loomkin_web/live/agent_card_component_test.exs --no-start (all approval_pending tests pass)
mix test (full suite green)
mix assets.build (no JS errors)
Human visual verification: approval gate card visually distinct from permission hook
</verification>

<success_criteria>
- :approval_pending card renders purple expanded panel with question and three action buttons
- Status dot is violet (bg-violet-500 animate-pulse), not amber
- card_state_class(:approval_pending) returns "agent-card-approval"
- Approve button sends approve_card_agent event; Deny sends deny_card_agent event
- Approve w/ Context and Deny textareas toggle on click (JS.toggle)
- CountdownTimer hook registered, ticks, and clears interval on destroyed()
- approval_gate_requested and approval_gate_resolved in @type_config with purple styling
- Leader banner renders when @leader_approval_pending is set
- Human confirms visual distinction from permission hook (amber vs purple)
- Full mix test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/06-approval-gates/06-04-SUMMARY.md`
</output>
