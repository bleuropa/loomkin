---
phase: 06-approval-gates
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - test/loomkin/tools/request_approval_test.exs
  - test/loomkin/teams/team_broadcaster_test.exs
  - test/loomkin_web/live/agent_card_component_test.exs
  - test/loomkin_web/live/workspace_live_approval_test.exs
autonomous: true
requirements:
  - INTV-02

must_haves:
  truths:
    - "Four test files exist with failing (red) test stubs for all approval gate behaviors"
    - "mix test on the four stub files produces compile-passes-but-test-fails output (no undefined module errors)"
    - "agent_card_component_test.exs has updated :approval_pending dot assertion (violet, not amber) that currently fails"
  artifacts:
    - path: "test/loomkin/tools/request_approval_test.exs"
      provides: "Failing stubs: blocking approval, response routing, timeout behavior"
    - path: "test/loomkin/teams/team_broadcaster_test.exs"
      provides: "Stub asserting approval signal types are in @critical_types"
    - path: "test/loomkin_web/live/agent_card_component_test.exs"
      provides: "Updated :approval_pending dot assertion + approval panel render stub"
    - path: "test/loomkin_web/live/workspace_live_approval_test.exs"
      provides: "Failing stubs: approve_card_agent event, deny_card_agent event, leader banner"
  key_links:
    - from: "test/loomkin/tools/request_approval_test.exs"
      to: "Loomkin.Tools.RequestApproval"
      via: "module reference in test"
      pattern: "RequestApproval"
    - from: "test/loomkin_web/live/workspace_live_approval_test.exs"
      to: "workspace_live approve_card_agent handler"
      via: "phx-click event simulation"
      pattern: "approve_card_agent"
---

<objective>
Create failing test stubs for all approval gate behaviors before any implementation exists.
These tests define the contract that Plans 02-04 must satisfy.

Purpose: Nyquist compliance — every implementation task has a pre-existing failing test.
Output: Four test files with stubbed assertions, all in red state.
</objective>

<execution_context>
@/Users/vinnymac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vinnymac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-approval-gates/06-CONTEXT.md
@.planning/phases/06-approval-gates/06-RESEARCH.md

<interfaces>
<!-- Canonical blocking tool pattern — RequestApproval mirrors this exactly -->
From lib/loomkin/tools/ask_user.ex:
```elixir
# Registry.register(Loomkin.Teams.AgentRegistry, {:ask_user, question_id}, caller)
# receive do
#   {:ask_user_answer, ^question_id, answer} -> ...
# after
#   300_000 -> ...
# end
```

From lib/loomkin/teams/team_broadcaster.ex:
```elixir
@critical_types MapSet.new([
  "team.permission.request",
  "team.ask_user.question",
  "team.ask_user.answered",
  "agent.error",
  "agent.escalation",
  "team.dissolved",
  "collaboration.peer.message",
  "agent.crashed",
  "agent.recovered",
  "agent.permanently_failed"
])
```

From lib/loomkin_web/live/agent_card_component.ex:
```elixir
defp card_state_class(_content_type, :approval_pending), do: "agent-card-blocked"  # must become "agent-card-approval"
defp status_dot_class(:approval_pending), do: "bg-amber-400 agent-dot-thinking"    # must become "bg-violet-500 animate-pulse"
defp status_label(:approval_pending), do: "Awaiting approval"
```

Existing test for reference:
test/loomkin/teams/team_broadcaster_test.exs — already exists, add an assertion
test/loomkin_web/live/agent_card_component_test.exs — already exists, update dot assertion
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create request_approval_test.exs and workspace_live_approval_test.exs stubs</name>
  <files>
    test/loomkin/tools/request_approval_test.exs,
    test/loomkin_web/live/workspace_live_approval_test.exs
  </files>
  <behavior>
    RequestApproval test stubs (all must initially fail with UndefinedFunctionError or flunk):
    - Test: module Loomkin.Tools.RequestApproval exists
    - Test: run/2 returns {:ok, %{status: :approved, context: _}} when response sent to registry before timeout
    - Test: run/2 returns {:ok, %{status: :denied, reason: :timeout}} after timeout_ms elapses
    - Test: registry unregistered after approval response received

    workspace_live_approval_test.exs stubs:
    - Test: approve_card_agent event routes response to blocking tool task via Registry
    - Test: deny_card_agent event routes denial with reason to tool task via Registry
    - Test: leader banner assign is set when ApprovalRequested signal arrives for agent with role :lead
    - Test: leader banner assign is cleared when ApprovalResolved signal arrives
  </behavior>
  <action>
    Create test/loomkin/tools/request_approval_test.exs. Use `ExUnit.Case` with `async: true`.
    Reference module `Loomkin.Tools.RequestApproval` (does not exist yet — tests will fail with UndefinedFunctionError or
    explicit `flunk` stubs). Use `flunk "not implemented"` for behavior stubs so the file compiles cleanly.

    Pattern from existing tests — look at test/loomkin/teams/agent_state_machine_test.exs for style.
    The timeout test should use a very short timeout (e.g., 50ms) via the optional `timeout` param to avoid slow tests.

    Create test/loomkin_web/live/workspace_live_approval_test.exs. Use `use LoomkinWeb.ConnCase`.
    Follow the pattern in test/loomkin_web/live/workspace_broadcast_test.exs.
    Use `flunk "not implemented"` stubs for each test that cannot yet pass.

    Do NOT attempt to make tests pass. The goal is: files compile, tests run, tests fail with clear messages.
  </action>
  <verify>
    <automated>cd /Users/vinnymac/Sites/vinnymac/loomkin && mix test test/loomkin/tools/request_approval_test.exs test/loomkin_web/live/workspace_live_approval_test.exs --no-start 2>&1 | tail -20</automated>
  </verify>
  <done>Both files compile without syntax errors; tests run and show failures (not compile errors)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update agent_card_component_test.exs and team_broadcaster_test.exs stubs</name>
  <files>
    test/loomkin_web/live/agent_card_component_test.exs,
    test/loomkin/teams/team_broadcaster_test.exs
  </files>
  <behavior>
    agent_card_component_test.exs changes:
    - Find the existing :approval_pending status dot test
    - Update assertion from "bg-amber-400" to "bg-violet-500" (this test now fails — good)
    - Add stub test: approval panel renders when card has pending_approval assign with gate_id, question, timeout_ms
    - Add stub test: card_state_class for :approval_pending uses "agent-card-approval" not "agent-card-blocked"

    team_broadcaster_test.exs changes:
    - Add test: TeamBroadcaster classifies "agent.approval.requested" as critical
    - Add test: TeamBroadcaster classifies "agent.approval.resolved" as critical
    - These tests fail until Plan 02 adds the types to @critical_types
  </behavior>
  <action>
    Read test/loomkin_web/live/agent_card_component_test.exs fully before editing.
    Find and update the approval_pending status dot assertion — change expected class from amber to violet.
    Add two new test blocks: one for approval panel render (flunk stub), one for card_state_class (assert fails against current "agent-card-blocked").

    Read test/loomkin/teams/team_broadcaster_test.exs fully before editing.
    Add a test block for critical_types coverage — assert that both approval signal type strings are members
    of the critical types set. Use `TeamBroadcaster.critical?/1` or inspect the module attribute directly via
    whichever pattern the existing tests use.

    Run `mix format` after all edits.
  </action>
  <verify>
    <automated>cd /Users/vinnymac/Sites/vinnymac/loomkin && mix test test/loomkin_web/live/agent_card_component_test.exs test/loomkin/teams/team_broadcaster_test.exs --no-start 2>&1 | tail -20</automated>
  </verify>
  <done>
    Both files compile and run. The updated approval_pending dot test fails (amber vs violet).
    The new broadcaster critical_type tests fail. Existing passing tests remain green.
  </done>
</task>

</tasks>

<verification>
mix test test/loomkin/tools/request_approval_test.exs test/loomkin/teams/team_broadcaster_test.exs test/loomkin_web/live/agent_card_component_test.exs test/loomkin_web/live/workspace_live_approval_test.exs --no-start

Expected: all four files compile cleanly, tests run with failures only from new stubs (no regressions in existing passing tests)
</verification>

<success_criteria>
- Four test files exist and compile
- Tests run (not crash with compile error)
- New approval-gate tests fail with assertion errors or flunk messages
- Pre-existing passing tests in agent_card_component_test.exs and team_broadcaster_test.exs remain green
- No watch-mode flags used
</success_criteria>

<output>
After completion, create `.planning/phases/06-approval-gates/06-01-SUMMARY.md`
</output>
